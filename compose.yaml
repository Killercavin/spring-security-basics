services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: gatelog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - gatelog:/var/lib/postgresql

volumes:
  gatelog: